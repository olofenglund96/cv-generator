<% options = {class: "form-control", disabled:true}%>
<%= disabled = @person.last_name.downcase == "Mildner".downcase %>
<%= simple_form_for(@person, html: { class: 'form-horizontal' }) do |f| %>
<%= f.input :first_name %>
  <% end %>