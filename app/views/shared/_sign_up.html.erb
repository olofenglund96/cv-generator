 <% if !user_signed_in? %>
            <%= link_to "Sign up", new_user_registration_path, class: "c-btn c-btn--alpha c-btn--sm" %>
          <%else%>
            <%= link_to("Sign out", destroy_user_session_path, method: :delete, class:"c-btn c-btn--alpha c-btn--sm") %>
          <%end%>  